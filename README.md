# sneakertail-gitops

Argo CD app-of-apps repo for Sneakertail.

## Current Scope

- `apps/dev/root.yaml` is the parent app
- `apps/dev/children/` contains the child apps for the split repos
- `apps/prod/` is intentionally left for later when prod manifests are ready
